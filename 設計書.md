# Z_Lib アーキテクチャ

[READMEへ戻る](README.md) · [API・利用ガイド](docs/api.md)

この文書は現行実装の構成を記載します。未実装の計画は [今後の改善](docs/roadmap.md) に分離しています。

## 処理の概要

ZIPをローカル作業領域にコピーして展開し、仮想パスを展開先へ解決します。フィルターがなければ全展開し、指定時は対象ファイルだけを展開します。Box APIへの接続や同期状態の取得は行いません。

```text
元ZIP → source_copy.zip → extracted/ → 仮想パス経由の読み書き
                                         ↓ commit
元ZIP ← 同じ親フォルダの一時ZIP ← staging_output.zip（再圧縮・検証）
```

## モジュールと責務

| モジュール | 責務 |
|---|---|
| `src/z_lib/core.py` | 公開API、ロード済みセッション管理、編集コンテキスト、終了時処理 |
| `src/z_lib/session.py` | ZIPごとの作業領域、モード、変更フラグ、保存・破棄 |
| `src/z_lib/path_resolver.py` | パス正規化、セッションの最長一致照合、実パスへの変換 |
| `src/z_lib/transfer.py` | ZIPコピー、存在・サイズ・更新時刻の取得、競合確認、置換 |
| `src/z_lib/backend/zipfile_backend.py` | 名前の復号、選択展開、展開先検証、再圧縮・CRC検証 |
| `src/z_lib/namespaces/` | 対応する標準ライブラリ操作のラッパーと変更ガード |
| `src/z_lib/exceptions.py` / `_types.py` | 例外と型定義 |

セッションはバックエンドの関数を直接呼びます。`ZipFileBackend` とプロトコル定義は存在しますが、`Z_Lib` のコンストラクターでバックエンドを差し替える仕組みはありません。

## セッションの作業領域

```text
workspace_dir または OSの一時領域/
└── z_lib_sess_*/
    ├── source_copy.zip       # ロード時のZIP。新規作成時は存在しない
    ├── extracted/            # 展開・編集先
    └── staging_output.zip    # コミット時の生成ZIP
```

必要容量には元ZIPのコピー、展開結果、保存時の生成ZIPが含まれます。さらに保存先の親フォルダにも一時ZIPを作成します。永続キャッシュや容量上限の管理はありません。

## 保存と状態

API経由の編集で `is_dirty=True`、`status="dirty"` になります。コミットでは元ZIPの競合確認、再圧縮、`testzip()` 検証、保存先フォルダへのコピー、`os.replace()` の順に実行します。成功後はメタデータを更新し、`is_dirty=False`、`status="committed"` になります。

圧縮・置換失敗では `status="failed"` と復旧候補パスを持つ例外を設定します。ただしコンテキスト終了やその後の操作によって作業領域の状態が変わるため、復旧は [APIガイド](docs/api.md#保存と復旧の制約) の制約に従ってください。

ロールバックの復元元は `source_copy.zip` です。コミット成功時にもこのコピーは更新されません。終了時の自動処理は変更済みセッションの保存を試み、例外を握りつぶすため、明示的な保存と解放が必要です。

## 文字コード・保持範囲

UTF-8フラグがあるエントリでは `ZipInfo.filename` を使用します。それ以外はLocal File Headerから名前の生バイト列を読み、CP932での復号を試みます。失敗時にはCP437経由の再復号などへフォールバックします。任意の文字コードを自動判別する仕組みではありません。

フィルターなしでは空ディレクトリを展開・再圧縮します。再圧縮は展開先から新しくZIPを生成するため、元ZIPのコメント、追加フィールド、圧縮方式、元の時刻・権限などのメタデータの同一保持は保証しません。

## 保証範囲

読み取り専用ガードはライブラリAPI経由のZIP内変更に対するものです。実パスにOSの書き込み制限を設定するものではありません。

展開時に対象パスが展開先を外れないか検査します。ただし展開サイズ・エントリ数の上限、暗号化ZIPへのパスワード指定、任意の外部変更やシンボリックリンクを含む実パス操作の隔離は提供していません。

置換には `os.replace()` を使いますが、クラウドへの同期完了、複数端末での排他制御、複数ZIPの一括確定は保証しません。性能は実データで計測する必要があります。
